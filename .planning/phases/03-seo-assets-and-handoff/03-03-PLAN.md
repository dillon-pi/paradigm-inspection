---
phase: 03-seo-assets-and-handoff
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CNAME
autonomous: true
requirements:
  - DEPLOY-02
  - DEPLOY-03

must_haves:
  truths:
    - "CNAME file exists at repo root containing exactly paradigminspection.org"
    - ".nojekyll file exists at repo root as an empty file"
    - "GitHub Pages will serve the site at the custom domain without Jekyll processing"
  artifacts:
    - path: "CNAME"
      provides: "Custom domain configuration for GitHub Pages"
      contains: "paradigminspection.org"
    - path: ".nojekyll"
      provides: "Disables Jekyll processing on GitHub Pages"
  key_links:
    - from: "CNAME"
      to: "GitHub Pages"
      via: "GitHub Pages custom domain mechanism"
      pattern: "paradigminspection\\.org"
---

<objective>
Create the CNAME file for GitHub Pages custom domain deployment and verify .nojekyll exists.

Purpose: GitHub Pages requires a CNAME file at repo root to serve the site at a custom domain. The .nojekyll file (created in Phase 1) prevents Jekyll processing. Both must be present for the site to resolve correctly at paradigminspection.org.

Output: CNAME file at repo root; verified .nojekyll file.
</objective>

<execution_context>
@C:/Users/Daddy/Downloads/Projects/paradigm_inspection/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Daddy/Downloads/Projects/paradigm_inspection/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-seo-assets-and-handoff/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CNAME file and verify .nojekyll</name>
  <files>CNAME</files>
  <read_first>
    - .planning/phases/03-seo-assets-and-handoff/03-CONTEXT.md (D-01 — domain is paradigminspection.org)
    - .nojekyll (verify it exists — Phase 1 DEPLOY-01 should have created it)
  </read_first>
  <action>
1. Create a file named `CNAME` at the repo root. The file must contain exactly one line with no trailing newline and no whitespace:
   ```
   paradigminspection.org
   ```
   Per D-01, the domain is `paradigminspection.org` (not .com). The CNAME file is how GitHub Pages knows to serve the site at this custom domain. Do NOT include `https://` or `www.` — just the bare domain.

2. Verify `.nojekyll` exists at the repo root. Phase 1 (DEPLOY-01) created this file. Check:
   - If `.nojekyll` exists and is empty (0 bytes): no action needed, note in summary "confirmed present"
   - If `.nojekyll` exists but has content: empty it (it must be a zero-byte file)
   - If `.nojekyll` is missing: create it as an empty file and note in summary "recreated — was missing"

Do NOT create `.nojekyll` blindly — check first to avoid an unnecessary git change.
  </action>
  <verify>
    <automated>test -f CNAME && cat CNAME | grep -q "^paradigminspection.org$" && test -f .nojekyll && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - CNAME file exists at repo root
    - CNAME file contains exactly one line: `paradigminspection.org`
    - CNAME file does NOT contain `https://`, `www.`, or any whitespace other than the domain
    - .nojekyll file exists at repo root
    - .nojekyll file is empty (0 bytes)
  </acceptance_criteria>
  <done>CNAME file committed at repo root with paradigminspection.org. .nojekyll confirmed present and empty. GitHub Pages deployment files are complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CNAME content | Controls which domain GitHub Pages serves the site on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | CNAME file | accept | Only repo collaborators can modify. GitHub Pages validates domain ownership via DNS. No external attack vector beyond repo access. |
| T-03-08 | Denial of Service | .nojekyll removal | accept | If accidentally deleted, Jekyll processing would break CSS/JS paths. Owner can re-add per HOW-TO-UPDATE.md instructions. Low probability. |
</threat_model>

<verification>
1. `cat CNAME` outputs exactly `paradigminspection.org` with no extra characters
2. `test -f .nojekyll && wc -c .nojekyll` confirms file exists and is 0 bytes
3. No other deployment-related files are missing from repo root
</verification>

<success_criteria>
- CNAME exists at repo root containing exactly `paradigminspection.org`
- .nojekyll exists at repo root as an empty file
- Both files are committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/03-seo-assets-and-handoff/03-03-SUMMARY.md`
</output>
